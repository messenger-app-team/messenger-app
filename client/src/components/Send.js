// this is send button
import React from "react";
import { Button } from "react-bootstrap";

const Send = () => {
    console.log("click")
};

return (
    <Button onClick={sendMesseng}>Send</Button>
);

export default Send;